'use client';

import React from 'react';

// This component is now just a placeholder to avoid breaking imports
// It doesn't render anything since we've removed light mode
const ThemeToggle: React.FC = () => {
  return null;
};

export default ThemeToggle;
